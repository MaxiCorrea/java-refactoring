/**
 * Dos subclases tienen el mismo campo/atributo
 * 
 * Suba el campo a la superclase!
 * 
 * Si dos subclases fueron desarrolladas de forma independiente o luego combinadas, puedes encontrar
 * caracteristicas duplicadas,en particular algunos campos pueden estar duplicados. Aveces los
 * campos tienen los mismos nombres pero no son lo mismos. Debes observar que codigo lo utiliza y si
 * se utiliza para los mismo que algun otro campos de una clase "hermana" podemos generalizar
 * subiendo el codigo a la clase padre.
 */
package dealingwithgeneralization.pullupfield;
