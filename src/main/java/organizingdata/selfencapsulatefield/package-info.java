/**
 * Está accediendo a un campo directamente, pero el acoplamiento con el campo se está volviendo
 * incómodo. Cree métodos de getters y setters para el campo y utilícelos solo para acceder
 * al campo.
 */
package organizingdata.selfencapsulatefield;
