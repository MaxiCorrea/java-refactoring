/**
 * Tiene un código de tipo que afecta el comportamiento de una clase, pero no puede usar subclases.
 * Reemplace el código de tipo con un objeto de estado.
 * 
 * Esto es similar a Reemplazar código de tipo con subclases, pero se puede usar si el código de
 * tipo cambia durante la vida del objeto o si otra razón impide la subclasificación. Utiliza el
 * patrón estatal o estratégico [Banda de los Cuatro]. El estado y la estrategia son muy similares,
 * por lo que la refactorización es la misma que use, y realmente no importa. Elija el patrón que
 * mejor se adapte a las circunstancias específicas. Si está tratando de simplificar un solo
 * algoritmo con Reemplazar condicional con polimorfismo, estrategia es el mejor término. Si va a
 * mover datos específicos de estado y piensa que el objeto cambia de estado, use el patrón de
 * estado.
 */
package organizingdata.replacetypecodewithstateorstrategy;
