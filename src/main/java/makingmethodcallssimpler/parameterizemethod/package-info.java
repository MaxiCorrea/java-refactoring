/**
 * Es posible que vea un par de métodos que hacen cosas similares pero varían según algunos valores.
 * En este caso, puede simplificar las cosas reemplazando los métodos separados con un método único
 * que maneja las variaciones por parámetros. Dicho cambio elimina el código duplicado y aumenta la
 * flexibilidad, porque puede lidiar con otras variaciones agregando parámetros.
 */
package makingmethodcallssimpler.parameterizemethod;
